import CreateContext from "./Context"

function messagesReducer(state, action) {}

function sendMessage(dispatch) {
  return (phone, text) => {

  }
}

export const { Context, Provider } = CreateContext(messagesReducer, [], [])
